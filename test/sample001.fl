(* 
	Sample program 
*)

program foo;

var 
	x,y,z: integer;

type
	something = array of char;
	a_func = function(x:integer): boolean;
	a_proc = procedure(a, b, g: array of char);

function nothing(): integer;
begin
	result := 10;
	return
end;


begin
	be:
	x:=1;
	y:=10;

	f:
	for z:=x to y do begin
		if z=5 then begin
			printf("HERE YOU ARE %d\n",nothing())
		end
	end;

	w:
	while x<>30 do begin
		x:=x+1;
		printf("X=%d\n",x)
	end;

	goto en;

	r:
	repeat 
	until 1<>1;

	en:
	printf("Exit")

end.
