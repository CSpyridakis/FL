(* 
	Sample program 
*)

program foo;

type
	arrayOfChar = array of char;
	a_func = function(x:integer): boolean;
	a_proc = procedure(a, b, g: array of char);

var 
	x,y,z: varArrayOfChar;
	a1,a2,a3: a_func;

type
	arrayOfInt = array [3] of int;
	arrayOfArray = array [3] of arrayOfInt;

var 
	b,b2,b3: arrayOfInt ;
	c1: array of arrayOfInt;
	d1,d2,d3: array [3] of arrayOfInt;
	

function nothing(): integer;
var 
	fb,fb2,fb3: arrayOfInt ;
	fc1: array of arrayOfInt;
	fd1,fd2,fd3: array [3] of arrayOfInt;

begin
	result := 10;
	result := 10;
	result := 10;
	result := 10;
	result := 10;
	if a=2 then
		return;
	return
end;


begin
	be:
	x:=1&&((2341)+((23)||4.2E-1))/34;
	y:=uygfkuyguyf+4;

	f:
	for z:=x to y do begin
		if z=5 then begin
			printf("HERE YOU ARE %d\n",nothing())
		end
	end;

	w:
	while x<>30 do begin
		x:=x+1;
		printf("X=%d\n",x)
	end;

	goto en;

	r:
	repeat 
	until 1<>1;

	en:
	printf("Exit")

end.
